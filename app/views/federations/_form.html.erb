<%= form_for(@federation, :html => { :multipart => true }) do |f| %>
	<% if @federation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@federation.errors.count, "error") %> prohibited this federation from being saved:</h2>

      <ul>
      <% @federation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
	  <%= f.label :name %><br>
	  <%= f.text_field :name %>
	</div>
	<div class="field">
	  <%= f.label :ip %><br>
	  <%= f.text_field :ip %>
	</div>
	<div class="field">
	  <%= f.label :site %><br>
	  <%= f.text_field :site %>
	</div>
	<div class="field">
	  <%= f.label :thumbnail %><br>
		<%= f.file_field :thumbnail %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
